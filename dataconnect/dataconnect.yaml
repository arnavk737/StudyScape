specVersion: "v1beta"
serviceId: "studyscape"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "studyscape-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
